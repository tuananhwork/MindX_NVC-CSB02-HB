queue = [1, 2, 3] # Tạo hàng đợi







# Thêm phần tử vào hàng đợi
queue.append(4) # Queue = [1, 2, 3, 4]

# Lấy phần tử ở đầu hàng đợi (Làm thay đổi queue)
queue.pop(0) # Queue = [2, 3, 4]

first = queue[0] # Không làm thay đổi queue, Queue = [1, 2, 3, 4]
